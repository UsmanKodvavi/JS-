    //    // Initialize the color array
    //    var colors = ["Red", "Green", "Blue", "Yellow"];

    //    // Function to display the array
    //    function displayColors() {
    //        document.getElementById("output").innerText = colors.join(", ");
    //    }

    //    // Initial display
    //    displayColors();

    //    // a. Add color to the beginning
    //    var newColor = prompt("What color do you want to add to the beginning?");
    //    colors.unshift(newColor);
    //    displayColors();

    //    // b. Add color to the end
    //    newColor = prompt("What color do you want to add to the end?");
    //    colors.push(newColor);
    //    displayColors();

    //    // c. Add two more colors to the beginning
    //    colors.unshift(prompt("Enter first color to add to the beginning"));
    //    colors.unshift(prompt("Enter second color to add to the beginning"));
    //    displayColors();

    //    // d. Delete the first color
    //    colors.shift();
    //    displayColors();

    //    // e. Delete the last color
    //    colors.pop();
    //    displayColors();

    //    // f. Add color at a specific index
    //    var index = prompt("At which index do you want to add a color?");
    //    newColor = prompt("Enter the color name to add:");
    //    colors.splice(index, 0, newColor);
    //    displayColors();

    //    // g. Delete color(s) from a specific index
    //    index = prompt("At which index do you want to delete color(s)?");
    //    var count = prompt("How many colors do you want to delete?");
    //    colors.splice(index, count);
    //    displayColors();

    // var score = [120 , 150 , 175 , 580 , 220];

    // document.write(score)  ; 

    //  score.sort();

    // console.log("score" , score);

    // document.write("score" , score);

    // var cities = ["karchi" , "lahore" , "multan" , "islamabad"];

    // var citiescopy = cities.slice(1 ,3 );

    // console.log( citiescopy , " citiescopy");

    // var sentence = ["this" , "is" , "my" , "cat"]

    // document.write(sentence)

    // sentence.toString